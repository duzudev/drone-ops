<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm fixed-top w-100">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <a class="navbar-brand" href="/">
      <img src="<%= asset_path('arco-logo.jpeg') %>" alt="Brand Logo" style="height: 25px;">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav flex-grow-1 justify-content-center">
        <li class="nav-item <%= 'active' if controller_name == 'dashboard' %>">
          <%= link_to 'Dashboard', dashboard_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'compliance' %>">
          <%= link_to 'Compliance', compliance_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'lms' %>">
          <%= link_to 'LMS', lms_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'projects' %>">
          <%= link_to 'Projects', projects_path, class: 'nav-link' %>
        </li>
      </ul>

      <!-- User login/register or email dropdown on the right -->
      <div class="d-flex align-items-center">
        <% if Current.user %>
          <div class="dropdown">
            <button class="btn btn-link dropdown-toggle" type="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              <%= Current.user.email %>
            </button>
            <ul class="dropdown-menu" aria-labelledby="profileDropdown">
              <li>
                <%= link_to "Edit Password", edit_password_path, class: "dropdown-item" %>
              </li>
              <li>
                <%= button_to "Logout", logout_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </div>
        <% else %>
          <%= link_to "Login", sign_in_path, class: "btn btn-primary me-2" %>
          <%= link_to "Register", sign_up_path, class: "nav-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
