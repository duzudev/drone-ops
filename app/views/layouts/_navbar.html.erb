<nav class="navbar navbar-expand-lg navbar-light fixed-top">
  <div class="container-fluid">
    <!-- Logo on the left -->
    <a class="navbar-brand" href="/">
      <img src="<%= asset_path('arco-logo.jpeg') %>" alt="Brand Logo" style="height: 25px;">
    </a>

    <!-- Toggle button for mobile view -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Links in the middle -->
      <ul class="navbar-nav mx-auto">
        <li class="nav-item <%= 'active' if controller_name == 'dashboard' %>">
          <%= link_to 'Dashboard', dashboard_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'compliance' %>">
          <%= link_to 'Compliance', compliance_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'lms' %>">
          <%= link_to 'LMS', lms_index_path, class: 'nav-link' %>
        </li>
        <li class="nav-item <%= 'active' if controller_name == 'projects' %>">
          <%= link_to 'Projects', projects_path, class: 'nav-link' %>
        </li>
      </ul>

      <!-- Notification icon -->
      <div class="mx-3">
        <%= link_to '<i class="bi bi-bell"></i>'.html_safe, '#', class: 'text-decoration-none text-secondary', title: 'Notifications' %>
      </div>

      <!-- Profile dropdown on the right -->
      <div class="dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false">
          <% if Current.user %>
            <%= Current.user.email %>
          <% else %>
            Profile
          <% end %>
        </button>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="profileDropdown">
          <% if Current.user %>
            <li><%= link_to 'Edit Password', edit_password_path, class: 'dropdown-item' %></li>
            <li><%= button_to 'Logout', logout_path, method: :delete, class: 'dropdown-item' %></li>
          <% else %>
            <li><%= link_to 'Login', sign_in_path, class: 'dropdown-item' %></li>
            <li><%= link_to 'Register', sign_up_path, class: 'dropdown-item' %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>

<%= javascript_importmap_tags %>

